{
  "name": "bar",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/bar/src",
  "prefix": "app",
  "projectType": "library",
  "tags": ["type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/libs/bar"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
